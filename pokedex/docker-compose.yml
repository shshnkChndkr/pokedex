version: '3.8'
services:
  web:
    build:
      context: ./
      target: dev
    volumes:
      - .:/src
    command: nodemon npm run start:dev
    ports:
      - "6000:6000"
    environment:
      NODE_ENV: development
      DEBUG: nodejs-docker-express:*